
&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Если Элементы.Список.ТекущийЭлемент.Имя = "ДатаЗакрытия" Тогда
		СтандартнаяОбработка = Ложь;
		СсылкаЗакрытие = Элементы.Список.ДанныеСтроки(ВыбраннаяСтрока).Закрытие;
		Попытка
			ОткрытьФорму("Документ.ЗакрытиеСмены.ФормаОбъекта", Новый Структура("Ключ", СсылкаЗакрытие));
		Исключение
		КонецПопытки;
	КонецЕсли;
	
КонецПроцедуры
