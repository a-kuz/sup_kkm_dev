
Процедура ДействияФормыКартинки(Кнопка)
	
	Если ЭтоНовый() Тогда
		Записать()
	КонецЕсли;
	
	ОбработкаКартинок = Обработки.КартинкиОбъектов.ПолучитьФорму();
	ОбработкаКартинок.Открыть();
	ОбработкаКартинок.ТипОбъекта	= Документы.Новость.ПустаяСсылка();
	ОбработкаКартинок.ТекОбъект		= Ссылка;
    ОбработкаКартинок.СформироватьОтчет();

КонецПроцедуры

Процедура WebСсылкаОткрытие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если НЕ ЗначениеЗаполнено(WebСсылка) Тогда
		Возврат;
	КонецЕсли; 
	
	Если НРег(Лев(WebСсылка,7)) <> "http://" Тогда
		WebСсылка = "http://" + WebСсылка;
	КонецЕсли; 
	
	ЗапуститьПриложение(WebСсылка);
	
КонецПроцедуры
