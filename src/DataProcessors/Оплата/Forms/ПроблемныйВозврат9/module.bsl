
Процедура ОтменаНажатие(Элемент)
	глОтсечкаПростоя();
	РезультатОплаты.Отмена = Истина;
	Закрыть(РезультатОплаты);
КонецПроцедуры

Процедура ОкНажатие(Элемент)
	глОтсечкаПростоя();
	Если ВозвратОплаты(докВозврат.ВариантОплаты = Справочники.ВариантыОплаты.Карта) Тогда
		Закрыть(РезультатОплаты);
	Иначе
		Закрыть(РезультатОплаты);
	КонецЕсли;

КонецПроцедуры

Процедура ПриОткрытии()
	Товары = докВозврат.Товары;
	Коплате = 0;
	НадписьПолучено = "" + Товары.Итог("Сумма") + " " + глСимволРубля;	
	ИнтерфейсРМ.ПриОткрытииОкна(ЭтаФорма);
КонецПроцедуры

Процедура ПриЗакрытии()
	ИнтерфейсРМ.ПриЗакрытииОкна();
КонецПроцедуры

Процедура ОплатаНаличнымиНажатие(Элемент)
	глОтсечкаПростоя();
	Если ВозвратОплаты() Тогда
		Закрыть(РезультатОплаты);
	Иначе
		Закрыть(РезультатОплаты);
	КонецЕсли;

КонецПроцедуры

НадписьПолучено = "0";
Получено = 0;
ПараметрыОкна = Новый Структура("Центр, Лево, Верх, Ширина, Высота", Истина);