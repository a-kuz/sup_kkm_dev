
Процедура СправочникСписокВыбор(Элемент, ВыбраннаяСтрока, Колонка, СтандартнаяОбработка)
//	ВыбраннаяСтрока.ПолучитьФОрму(ВыбраннаяСтрока.Форма).Открыть();
КонецПроцедуры


Попытка
	с = РаботаСокнами.AHK();
Исключение
	ПутьDLL = ПолучитьИмяВременногоФайла("dll");
	Справочники.ФормыДиалогов.ПолучитьМакет("Autohotkey").Записать(ПутьDLL);
	WshShell.run("regsvr32.exe " + ПутьDLL + " /s",1,0);
КонецПопытки;

